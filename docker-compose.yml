version: '3.4'

services:
  divartelegrambot:
    image: divartelegrambot
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - ./.env
    volumes:
      # Persist seen ads on the host (and so `cat tokens.json` matches the bot)
      - ./tokens.json:/app/tokens.json:Z
