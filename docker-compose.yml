version: '3.4'

services:
  divartelegrambot:
    image: divartelegrambot
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - ./.env
    volumes:
      # Mount a directory (file bind-mounts break atomic token saves)
      - ./data:/app/data:Z
